import 'package:flutter/material.dart';
import 'pantalla_principal.dart';

// Exporta la pantalla principal con su nombre original para mantener compatibilidad
class VisitorDashboardScreen extends PantallaPrincipalVisitante {
  const VisitorDashboardScreen({Key? key}) : super(key: key);
}
